PySide6>=6.6
mediapipe>=0.10
# opencv-python-headless, not opencv-python: the regular package bundles its own Qt
# build for GUI functions (imshow, etc.) that we never use, and having two separate Qt
# runtimes (that one + PySide6) in one process causes QObject thread-affinity conflicts.
opencv-python-headless>=4.8
